/**
 * Cette page permet d'avoir du style en global qui affectera tous le site
 */
import '../styles/global.css'

export default function App ({ Component, pageProps }) {
  return <Component {...pageProps} />
}